/**
 * Exporting all the event files here ./events/*
 * Now, when we need an event all we have to do is:
 * import { someEvent } from '@libs/tagular';
 */
export { default as TagularClick } from './TagularClick';
export { default as TagularViewed } from './TagularViewed';
